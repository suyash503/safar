# ---------------------------------------------------------------------------
# Secrets
# ---------------------------------------------------------------------------
# The Supabase ANON key is safe to commit — it's designed to be public and RLS
# is what protects the data. The SERVICE_ROLE key is not: it bypasses every
# policy in backend/schema.sql. Keep both in .env anyway; habits beat judgement.
.env
.env.*
!.env.example
google-services.json
*.keystore
*.jks

# ---------------------------------------------------------------------------
# Fonts
# ---------------------------------------------------------------------------
# Only ship faces we're licensed to redistribute. Jangkuy is committed on
# purpose — free for commercial use, licence in fonts/. Anything else stays out.
*.zip
fonts/*
!fonts/JANGKUY-*

# ---------------------------------------------------------------------------
# Node / Expo / React Native  (arrives with the app scaffold)
# ---------------------------------------------------------------------------
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# Native build output
android/app/build/
android/build/
android/.gradle/
ios/build/
ios/Pods/
*.apk
*.aab
*.ipa

# ---------------------------------------------------------------------------
# Editors and OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# throwaway auth diagnostic — holds project keys while testing
test-auth.html

# --- added by Push-Safar.ps1 ---
node_modules/
npm-debug.log
yarn-error.log

# Android generated
android/build/
android/app/build/
android/.gradle/
android/.idea/
android/local.properties
android/app/release/
*.apk
*.aab
*.keystore
!debug.keystore

# iOS generated
ios/Pods/
ios/build/
ios/*.xcworkspace/xcuserdata/

# Expo / Metro
.expo/
.expo-shared/
.metro-health-check*

# Secrets
.env
.env.*
!.env.example
google-services.json
GoogleService-Info.plist

# Misc
.DS_Store
Thumbs.db
coverage/
