# Normalise line endings so Windows checkouts don't produce whole-file diffs.
* text=auto eol=lf

# Binary — never touch these
*.otf  binary
*.ttf  binary
*.woff binary
*.woff2 binary
*.png  binary
*.jpg  binary
*.zip  binary
